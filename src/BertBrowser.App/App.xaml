<Application x:Class="BertBrowser.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             ShutdownMode="OnMainWindowClose">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- Icons first, then theme, then styles. StaticResource is looked up at parse time
                     and only sees dictionaries merged before it, so this order is the dependency
                     order: Icons.xaml is pure geometry and depends on nothing, the window chrome
                     inside Theme.xaml draws icons, and Styles.xaml resolves colours from the theme
                     tokens. -->
                <ResourceDictionary Source="Resources/Icons.xaml"/>
                <ResourceDictionary Source="Resources/Theme/Theme.xaml"/>
                <ResourceDictionary Source="Resources/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Application.Resources>
</Application>
