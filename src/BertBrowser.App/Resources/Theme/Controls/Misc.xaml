<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Primitives.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <Style TargetType="StatusBar">
        <Setter Property="Background" Value="{StaticResource Theme.StatusBar.Background}"/>
        <Setter Property="Foreground" Value="{StaticResource Theme.StatusBar.Foreground}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="8,0"/>
        <Setter Property="MinHeight" Value="24"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="StatusBar">
                    <Border Background="{TemplateBinding Background}"
                            Padding="{TemplateBinding Padding}"
                            MinHeight="{TemplateBinding MinHeight}"
                            SnapsToDevicePixels="True">
                        <ItemsPresenter/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="StatusBarItem">
        <Setter Property="Foreground" Value="{StaticResource Theme.StatusBar.Foreground}"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="StatusBarItem">
                    <!-- No separator of its own: the items are laid out by the status bar's own
                         DockPanel and space between them is set per item. -->
                    <ContentPresenter Margin="{TemplateBinding Padding}"
                                      VerticalAlignment="Center"
                                      TextBlock.Foreground="{TemplateBinding Foreground}"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--
      Determinate progress is fully retemplated. The indeterminate case deliberately keeps WPF's own
      animation and only takes the colours: a custom marquee needs its travel bound to ActualWidth,
      which an in-template Storyboard (frozen at parse) cannot do — it would need code in
      OnApplyTemplate. Not worth it for a 3px activity strip.
    -->
    <Style TargetType="ProgressBar">
        <Setter Property="Background" Value="{StaticResource Theme.Progress.Track}"/>
        <Setter Property="Foreground" Value="{StaticResource Theme.Progress.Indicator}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Height" Value="4"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Style.Triggers>
            <Trigger Property="IsIndeterminate" Value="False">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ProgressBar">
                            <Border Background="{TemplateBinding Background}" CornerRadius="2"
                                    SnapsToDevicePixels="True">
                                <Grid ClipToBounds="True">
                                    <Rectangle x:Name="PART_Track"/>
                                    <Rectangle x:Name="PART_Indicator" HorizontalAlignment="Left"
                                               RadiusX="2" RadiusY="2"
                                               Fill="{TemplateBinding Foreground}"/>
                                </Grid>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!--
      VS Code's sash: invisible until you reach for it. Used by the sidebar splitter in XAML and by
      every splitter PaneLayoutHost builds in code.
    -->
    <Style x:Key="PaneSplitterStyle" TargetType="GridSplitter">
        <Setter Property="Background" Value="{StaticResource Theme.Splitter.Background}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="ShowsPreview" Value="False"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="GridSplitter">
                    <Border x:Name="Bd" Background="{TemplateBinding Background}"
                            SnapsToDevicePixels="True"/>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background"
                                    Value="{StaticResource Theme.Splitter.HoverBackground}"/>
                        </Trigger>
                        <Trigger Property="IsDragging" Value="True">
                            <Setter TargetName="Bd" Property="Background"
                                    Value="{StaticResource Theme.Splitter.HoverBackground}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="GridSplitter" BasedOn="{StaticResource PaneSplitterStyle}"/>

    <!--
      Deliberately no Foreground here. TextElement.Foreground is an inherited property, and a
      setter on the implicit style beats inheritance — which would repaint the text on a selected
      row, in the status bar, and under a highlighted menu item back to the body colour. Containers
      set Foreground; TextBlocks inherit it.
    -->
    <Style TargetType="TextBlock">
        <Setter Property="TextOptions.TextFormattingMode" Value="Display"/>
    </Style>
</ResourceDictionary>
