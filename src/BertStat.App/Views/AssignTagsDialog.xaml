<Window x:Class="BertStat.App.Views.AssignTagsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}" Width="380" Height="440"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
    <DockPanel Margin="10">
        <!-- New tag row -->
        <Grid DockPanel.Dock="Top" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Text="{Binding NewTagName, UpdateSourceTrigger=PropertyChanged}"
                     VerticalContentAlignment="Center" Margin="0,0,6,0"
                     KeyDown="NewTagBox_KeyDown" ToolTip="Create a new tag and check it"/>
            <Button Grid.Column="1" Content="New tag" Padding="12,4" Command="{Binding CreateTagCommand}"/>
        </Grid>

        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="OK" Width="80" Margin="0,0,6,0" IsDefault="True" Click="Ok_Click"/>
            <Button Content="Cancel" Width="80" IsCancel="True"/>
        </StackPanel>

        <ListBox ItemsSource="{Binding Tags}" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay}" IsThreeState="{Binding OriginalState, Converter={StaticResource IsNullConverter}}"
                              Margin="2">
                        <StackPanel Orientation="Horizontal">
                            <Border Background="{Binding Tag.Color, Converter={StaticResource TagColor}}"
                                    Width="12" Height="12" CornerRadius="6" Margin="0,0,6,0" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding Tag.Name}" VerticalAlignment="Center"/>
                        </StackPanel>
                    </CheckBox>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </DockPanel>
</Window>
